{
  "name": "yt-downloader-railway",
  "version": "1.0.0",
  "description": "Téléchargeur YouTube avec ytdl-core et fallback yt-dlp CLI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "apt-get update && apt-get install -y yt-dlp"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ytdl-core": "^4.11.5"
  },
  "engines": {
    "node": ">=16"
  }
}
